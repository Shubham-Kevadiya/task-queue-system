// import express from "express";
// import { createTask, getTaskStatus } from "../controller/task.controller.js";
// import { createTaskValidation } from "../middleware/body.validate.js";
// const taskRoute = express.Router();

// taskRoute.post("/", createTaskValidation, createTask);
// taskRoute.get("/:jobId", getTaskStatus);

// export default taskRoute;
